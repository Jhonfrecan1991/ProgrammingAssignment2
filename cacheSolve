cacheSolve <- function(X) {
  inv <- X$Inver()
  if(identical(inv, solve(X$Mat()))) {
    message("Obtención de inversa en caché")
    return(inv)
  }
  else{
    Matr <- X$Mat()
    inv <- solve(Matr)
    X$Inversa(inv)
    return(inv)
  }
}
